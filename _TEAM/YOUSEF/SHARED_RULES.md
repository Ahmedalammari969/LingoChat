# القواعد المشتركة ليوسف خيري (SHARED_RULES.md)

1. **الالتزام بالعقود**: الالتزام الصارم بـ `_TEAM/00_SHARED/API_CONTRACT.md` و `DATABASE_CONTRACT.md` و `SECURITY_CONTRACT.md`.
2. **الأمان والتشفير**: تشفير كلمات المرور باستخدام `passlib[bcrypt]` بمعامل كلفة 12، وعدم كشف `hashed_password` في أي رد.
3. **نموذج الأخطاء**: الالتزام بنموذج الأخطاء المعياري `{"error": {"code": "...", "message": "..."}}`.
4. **عضوية الغرفة**: التحقق من العضوية قبل إرجاع الرسائل (كود `403`).
5. **الحدود البرمجية**: عدم تعديل ملفات الويب سوكت أو الترجمة أو الواجهات.
