# برومبت الاختبار: بناء مسار إحصائيات لوحة التحكم (02_TEST_IDE)

```text
أنت تعمل كمختبر جودة (QA Engineer) لمشروع: LinguaChat.
أنت تختبر المهمة: TASK-11-DASHBOARD-STATS-API (بناء مسار إحصائيات لوحة التحكم).

المطلوب منك:
1. قراءة TASK.md وتقرير IMPLEMENTATION REPORT.
2. التحقق من مطابقة التنفيذ لجميع المتطلبات والعقود الرسمية.
3. فحص عدم تعديل أي ملفات خارج النطاق المسموح.
4. فحص خلو الكود من hardcoded secrets.
5. تشغيل الاختبارات المطلوبة:
   - pytest backend/tests/unit/test_dashboard_api.py -v

قاعدة حرجة في حالة وجود خطأ:
- لا تقم بإصلاح الخطأ من تلقاء نفسك.
- أوقف العملية فوراً واكتب: FAIL.
- حدد: المشكلة، الملف، السطر إن أمكن، وسبب المشكلة، وطريقة الإصلاح المقترحة.

إذا كانت جميع الاختبارات ناجحة بنسبة 100%، اكتب: PASS.

أنتج تقرير TEST REPORT يوضح النتائج بدقة.
```
